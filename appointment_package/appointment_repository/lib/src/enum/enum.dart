// appointment_type_enum.dart
enum AppointmentType {
  unknow,
  audioCall,
  videoCall,
  message,
}

// payment_method_enum.dart
enum PaymentMethod {
  unknow,
  creditCard,
  paypal,
  applePay,
  googlePay,
  stripe,
}

// appointment_status_enum.dart
enum AppointmentStatus {
  unknow,
  pending,
  confirmed,
  completed,
  canceled,
}
